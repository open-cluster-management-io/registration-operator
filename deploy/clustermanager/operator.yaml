kind: Deployment
apiVersion: apps/v1
metadata:
  name: clustermanager
  namespace: open-cluster-management
  labels:
    app: clustermanager
spec:
  replicas: 3
  selector:
    matchLabels:
      app: clustermanager
  template:
    metadata:
      labels:
        app: clustermanager
    spec:
      serviceAccountName: clustermanager
      containers:
      - name: nucleus-operator
        image: quay.io/open-cluster-management/nucleus:latest
        imagePullPolicy: IfNotPresent
        args:
          - "/nucleus"
          - "hub"
        livenessProbe:
          httpGet:
            path: /healthz
            scheme: HTTPS
            port: 8443
          initialDelaySeconds: 2
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            scheme: HTTPS
            port: 8443
          initialDelaySeconds: 2
